# DecisionLift UI

Frontend for DecisionLift. React and TypeScript on Vite, styled with Tailwind, shipped with Docker and nginx.

## What is in here

- Vite + React + TypeScript app
- Tailwind UI
- `Dockerfile` and `docker-compose.yml` for a production nginx image

## Stack

React, TypeScript, Vite, Tailwind, Docker, nginx
